$ErrorActionPreference = "Stop"
Set-StrictMode -Version 2.0

# Compatibility stub. The hand-carried Windows RC bundle was retired when the
# bootstrap flow changed to OSS Clash -> subscription checkpoint -> official
# Anthropic Claude Code download through the verified proxy.
throw "windows-local-rc.ps1 is retired; use the fixed OSS bootstrap.ps1 entry after stable promotion"
